# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local daemon state
.mentor/

# Built dashboard (generated by `npm run build` in frontend/ — see
# frontend/vite.config.ts). Ship it in the wheel via package-data, but
# don't commit generated build output.
backend/daemon/static/

# Local configuration (API keys)
.env
